# spec: [기능명] ([티켓 번호])

## 1. 기능 개요

[2-3문장으로 기능의 목적과 배경 설명]

### 화면 구성 / 기능 구성

| 섹션 / 기능 | 표시 데이터 / 설명 |
|------------|------------------|
| **[섹션명]** | [데이터 항목] |
| **[섹션명]** | [데이터 항목] |

---

## 2. API 설계

### API 1 - [API 이름]

```
[HTTP METHOD] [/path/to/endpoint]
Authorization: Bearer {token}
```

- [이 API가 하는 일 한 줄 설명]

**Request**
```json
{
  "field1": "value",
  "field2": 0
}
```

**Response 200 OK**
```json
{
  "field1": "value",
  "field2": 0
}
```

**엣지 케이스 (예: 데이터 없을 경우)**
```json
{
  "field1": null
}
```

---

### API 2 - [API 이름] (필요 시)

```
[HTTP METHOD] [/path/to/endpoint]
```

**Response 200 OK**
```json
{}
```

---

## 3. 비즈니스 로직

### 3-1. [주요 로직 1]

- [처리 흐름 설명]
- [사용할 서비스/메서드]
- [예외 케이스]

### 3-2. [주요 로직 2]

- [처리 흐름 설명]
- [사용할 서비스/메서드]

### 3-3. [상태값 / 매핑 규칙] (필요 시)

| 내부 값 (enum/DB) | 표시 / 변환 값 |
|-------------------|--------------|
| [VALUE_A]         | [표시 문자열] |
| [VALUE_B]         | [표시 문자열] |

---

## 4. 구현 대상 파일

### [모듈명] 모듈

| 파일 | 역할 |
|------|------|
| `[ClassName]` | [역할 설명] |
| `[ClassName]` | [역할 설명] |

### 패키지 위치

```
[모듈]/src/main/java/[패키지경로]/
├── [domain]/
│   ├── query/
│   │   ├── api/
│   │   │   ├── [Controller].java        (신규/수정)
│   │   │   └── dto/
│   │   │       └── [ResponseDTO].java   (신규/수정)
│   │   └── executor/
│   │       └── [Executor].java          (신규/수정)
│   └── command/  (필요 시)
│       ├── api/
│       └── executor/
```

### [다른 모듈명] 모듈 변경사항

- [변경 내용] 또는 "변경 없음 — 기존 서비스 재활용"
  - `[ServiceClass.method()]` ← [용도]

---

## 5. 주요 고려사항 & 질문

1. **[고려사항 제목]**: [내용 및 확인 필요 여부]
   - 대안: [대안 설명]

2. **[고려사항 제목]**: [내용]

3. **[고려사항 제목]**: [내용]

---

## 6. 구현 순서 (TDD)

1. [ ] [첫 번째 구현 대상] — [간단한 설명]
2. [ ] [두 번째 구현 대상] — [간단한 설명]
3. [ ] [테스트 작성] — [테스트 범위]
4. [ ] [통합 테스트] — [검증 항목]

---

## 7. 인수 조건 (Acceptance Criteria)

- [ ] [조건 1]
- [ ] [조건 2]
- [ ] [조건 3]
